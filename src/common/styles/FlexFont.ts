import styled from "@emotion/styled"
import { Typography } from "@mui/material"

const FlexFont = styled(Typography)`
  font-size: clamp(1rem, 5vw, 4rem);
  text-align: center;
`
export default FlexFont
